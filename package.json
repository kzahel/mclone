{
  "name": "mclone",
  "version": "0.0.0",
  "private": true,
  "description": "Web-based Minecraft-inspired voxel sandbox (private, personal use).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "oracle:gen": "bash ./oracle/run.sh"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.1"
}
